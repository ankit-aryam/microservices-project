spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/order_db
    username: postgres
    password: Ankit@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8083

